import { IQuote } from "@/common/types/blocks/quote.types"

const Quote = ({ _type, editor, quote }: IQuote) => {
    return (
        <div>
            QUOTE BLOCK
        </div>
    )
}

export default Quote;